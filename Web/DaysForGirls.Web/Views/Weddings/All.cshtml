@model List<ProductDisplayAllViewModel>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "All";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*@ViewData["productType"]  = @Model[0].ProductType

@if ((string)this.ViewData["productType"] == "Dress")
{
    <h1 class="text-center">Enjoy our Wedding Dresses</h1>
}
else if ((string)this.ViewData["productType"] == "Suit")
{
    <h1 class="text-center">Enjoy our Wedding Suits</h1>
}*@

<style>
    img .image-thumbnail {
        border: 1px solid #ddd; /* Gray border */
        border-radius: 4px; /* Rounded border */
        padding: 5px; /* Some padding */
        width: 150px; /* Set a small width */
    }

    /* Add a hover effect (blue shadow) */
    img .image:hover{
        box-shadow: 0 0 2px 1px rgba(0, 140, 186, 0.5);
    }

    img .product-image{
        width: 300px;
        height: 200px;
    }
</style>

<h1 class="text-center">All Wedding Products</h1>

    <div @*id="products" class="mt-3"*@>
        @for (int i = 0; i < Model.Count; i += 4)
        {
            <div @*class="products-row row d-flex justify-content-around my-5"*@>
                @for (int j = i; j < i + 4; j++)
                {
                    if (j < Model.Count)
                    {
                        <div @*class="product col-md-2"*@>
                            <div @*class="image-holder d-flex justify-content-center"*@>
                                <a href="/Products/Details/@Model[j].Id">
                                    <img class="image-thumbnail product-image" src="@Model[j].MainPicture" alt="@Model[j].Name">
                                </a>
                            </div>
                            <h5 class="text-primary text-center mt-3">@Model[j].Name</h5>
                            <h5 class="text-primary text-center mt-3">Price: &euro;@Model[j].Price.ToString("F2")</h5>
                        </div>
                    }
                    else
                    {
                        <div class="product col-md-2 empty">
                        </div>
                    }
                }
            </div>
        }
    </div>

@*<div class="container-fluid">
    @foreach (var product in Model)
    {
        <div class="col-md-3">
            <a asp-controller="" asp-action="Details/@product.Id"><img src="@product.MainPicture" alt="@product.Name" /></a>
            <h3>@product.Name</h3>
            <h3>&euro;@product.Price</h3>
            @if (product.Quantity < 1)
            {
                <h3>Sold Out</h3>
            }
        </div>
    }
</div>*@
